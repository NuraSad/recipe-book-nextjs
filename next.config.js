const nextConfig = {};

module.exports = nextConfig;

module.exports = {
  images: {
    remotePatterns: [
      {
        hostname: "images.unsplash.com",
        //in my case i used cdn.pixabay.com
      },
    ],
  },
};
